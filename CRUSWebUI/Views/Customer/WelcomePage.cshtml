@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@* This tells you that this view will expect to retrieve a list of RestaurantVM *@
@model IEnumerable<CRUSWebUI.Models.CustomerVM>

    <h1 class="display-4">Welcome @ViewData["CustomerName"]  </h1>
    @*<br />
        <h1>@ViewBag["Customer Name"]</h1>
        <h1>@ViewBag["Customer Email"]</h1>*@




    <br /> <!-- This br tag will space between elements -->
    <br />

    <h3>Thank you for Choosing our Store @ViewData["CustomerName"]. In the Drop Down</h3>
    <h3>menu below, Please Choose next where you would like to go.</h3>




    <a class="btn btn-primary text-white" asp-controller="OrderPlacement" asp-action="Create">Add to Order</a>
    <br /> <!-- This br tag will space between elements -->
    <br />

    <h1 class="display-4">Order History</h1>
    <table class="table table-striped table-hover">
        <tr>
            <th>Order ID</th>
            <th>StoreFront ID</th>
            <th>Customer ID</th>
            <th>Total_Price</th>
        </tr>
        @*@foreach (OrderPlacementVM item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.Address)</td>
                <td>@Html.DisplayFor(modelItem => item.CustomerId)</td>*@
                <td>
                    <!--First parameter of ActionLink is the name it will give for this element -->
                    <!--Second parameter will point to the action of the restaurant controller-->
                    <!--Third parameter will give the information from the view to the controller-->
                    <button class="btn btn-danger">@Html.ActionLink("StoreFronts", "Index", "StoreFront", new { @style = "color: white;" })</button>
                    <button class="btn btn-primary">@Html.ActionLink("Login", "Details", new { @style = "color: white" })</button>
                </td>
            </tr>
        }
    </table>
