@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model CRUSWebUI.Models.StoreFrontVM

<h1 class="display-4">Add A StoreFront</h1>
<h3>You sure you want to Add This store?</h3>

<div>
    <dl>
        <dt>
            Id:
            <span class="col-sm-10" style="font-weight: normal;">@Html.DisplayFor(model => model.StoreFrontId)</span>
        </dt>
        <dt>
            Name:
            <span class="col-sm-10" style="font-weight: normal;">@Html.DisplayFor(model => model.Name)</span>
        </dt>
        <dt>
            Email:
            <span class="col-sm-10" style="font-weight: normal;">@Html.DisplayFor(model => model.Description)</span>
        </dt>
        <dt>
            Address:
            <span class="col-sm-10" style="font-weight: normal;">@Html.DisplayFor(model => model.Address)</span>
        </dt>

        <!--We need a form to do a post request-->
        <form asp-action="">
            <!--
                This input element is hidden because we don't want the user to input some id instead we are just supplying the id w
                Already have
            -->
            <input type="hidden" asp-for="StoreFrontId" />
            <input type="submit" value="Create" class="btn btn-danger" />
        </form>
    </dl>
</div>
